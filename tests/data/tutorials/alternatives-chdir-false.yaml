# Tutorial that has a foo and a bar alternative, but also uses chdir
parts:
  - alternatives:
      foo:
        commands:
          - command: ls foo
            chdir: false
      bar:
        commands:
          - command: ls bar
            chdir: false
    chdir: false
  - commands:
      - command: ls