configuration:
  context:
    stdin_test_path: /tmp/stdin.test
  run:
    runner:
      path: structured_tutorials.runners.vagrant.VagrantRunner
      options:
        prepare_box:
          path: box
          name: prepared-box
parts:
  - commands:
      - command: ls