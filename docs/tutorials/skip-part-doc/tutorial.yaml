parts:
  - commands:
      - command: ls /tmp
        doc:
          output: ...
    # We could also skip part at runtime instead:
    #run: false
    doc: false
  - commands:
      - command: ls /etc
        doc:
          output: ...