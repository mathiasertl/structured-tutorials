configuration:
  run:
    runner:
      path: structured_tutorials.runners.vagrant.VagrantRunner
      options:
        environment:
          VAGRANT_CWD: subdir
parts:
  - commands:
      - command: ls